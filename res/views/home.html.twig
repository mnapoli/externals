{% extends '/app/views/layout.html.twig' %}

{% block content %}

    <div class="page-header">
        <h1>Hello</h1>
    </div>

    {% for thread in threads %}
        <p><a href="thread/{{ thread.id }}">{{ thread.subject }}</a> {{ thread.lastUpdate }}</p>
    {% endfor %}

{% endblock %}
