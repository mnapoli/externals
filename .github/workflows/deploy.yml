name: Deploy

on:
    push:
        branches: ['master']

jobs:

    deploy:
        runs-on: ubuntu-latest
        # Avoid deploying concurrently
        concurrency: deploy
        timeout-minutes: 30
        steps:
            -   uses: actions/checkout@v6

            -   uses: actions/setup-node@v4
                with:
                    node-version: '22'
                    cache: 'npm'

            -   name: Setup PHP
                uses: shivammathur/setup-php@v2
                with:
                    php-version: '8.5'
                    tools: bref/cli
                    coverage: none

            -   uses: ramsey/composer-install@v4
                with:
                    composer-options: '--no-dev'

            -   run: npm ci

            -   run: npm run build

            -   run: bref deploy --env=prod
                env:
                    BREF_TOKEN: ${{ secrets.BREF_TOKEN }}
