# Database Configuration
# For SQLite: set DATABASE_PATH (takes priority if set)
# DATABASE_PATH=/path/to/externals_dev.db

# For MySQL: leave DATABASE_PATH unset and configure these:
DATABASE_NAME=externals
DATABASE_USER=root
DATABASE_PASSWORD=
DATABASE_HOST=localhost
DATABASE_PORT=

GITHUB_OAUTH_CLIENT_ID=
GITHUB_OAUTH_CLIENT_SECRET=
GITHUB_OAUTH_REDIRECT_URL=http://localhost:8000/login

ALGOLIA_APP_ID=
ALGOLIA_API_KEY=
ALGOLIA_INDEX_PREFIX=dev_

SESSION_SECRET_KEY=abcdef
RSS_HOST=http://localhost:8000
